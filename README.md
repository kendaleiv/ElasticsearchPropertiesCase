# ElasticsearchPropertiesCase

Testing scenarios for character case of properties in Elasticsearch (using NEST) with .NET Framework / .NET Core -- see `Tests.cs`.

```
> dotnet build
> dotnet test
```
